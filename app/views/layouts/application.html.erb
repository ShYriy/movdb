<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Movdb</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="p-3 bg-dark text-white">
      <div class="container">
        <nav class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
          <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
            <li><%= link_to 'Main', root_path, class: 'nav-link px-2 text-white' %></li>
            <li><%= link_to 'Movies', movies_path, class: 'nav-link px-2 text-white' %></li>
            <% if current_user %>
              <%= link_to 'Sign Out', destroy_user_session_path, method: :delete, class: 'nav-link px-2 text-white' %>
            <% else %>
              <li><%= link_to 'Sign Up', new_user_registration_path, class: 'nav-link px-2 text-white' %></li>
              <li><%= link_to 'Sign In', new_user_session_path, class: 'nav-link px-2 text-white' %></li>
            <% end %>
          </ul>
          <% if current_user %>
            User: <%= current_user.email %>
          <% end %>
        </nav>
      </div>
    </header>

    <main class="container mt-3">
      <% flash.each do |k, v| %>
        <%= tag.div v, class: "alert alert-#{k}", role: 'alert' %>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
